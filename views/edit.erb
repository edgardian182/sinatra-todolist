<% if @tarea %>
  <h1>Edición de Tarea</h1>
  <form role="form" action="/<%= @tarea["id"] %>/edit" method="post">
    <div class="form-group">
      <input class="form-control" type="text" name="title" placeholder="Nuevo texto...">
    </div>
    <input class="btn btn-primary" type="submit">
  </form>
<% else %>
  <p class="texto-visible">Tarea no encontrada</p>
<% end %>